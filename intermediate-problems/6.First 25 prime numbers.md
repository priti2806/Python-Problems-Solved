def prime_numbers():
    result = []
    n = 2

    while len(result) < 25:
        for i in range(2, n):
            if n % i == 0:
                break
        else:
            result.append(n)

        n += 1

    return result


print(prime_numbers())
