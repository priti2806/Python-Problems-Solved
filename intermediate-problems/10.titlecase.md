Write a python program to convert a string to title case without using the title()

s = input("Enter the String : ")
words = s.split()

result = []

for word in words:
    result.append(word[0].upper() + word[1:].lower() )
    
print("Titlecase : ", " ".join(result))